# The platform root currently requires no environment variables.
# Keep product, provider, database, and deployment secrets in the owning child
# repository instead of sharing one platform-wide env file.
